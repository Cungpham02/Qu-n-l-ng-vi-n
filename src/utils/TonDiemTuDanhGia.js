export const calculateTotalScore = (phieudanhGiaByDotAndByIdDangVien) => {
  let totalScore = 0;
  phieudanhGiaByDotAndByIdDangVien.forEach((item) => {
    console.log(item.dvdg);
  });
  return totalScore;
};
